# API Endpoints

- `POST /api/analyze` - Request profile scraping and analysis for a given username.
- `POST /api/recommend/:type` - Retrieve recommendations for a specific recommendation engine.
- `GET /api/status/:username` - Check cache and analysis status for a user profile.
- `GET /health` - System health check.
